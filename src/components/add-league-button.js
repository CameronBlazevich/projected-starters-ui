import { Button } from "reactstrap";
const AddLeagueButton = (props) => {
    return (<Button size="sm" color="primary" onClick={props.onClick}>Add League</Button>)
  }

  export default AddLeagueButton;